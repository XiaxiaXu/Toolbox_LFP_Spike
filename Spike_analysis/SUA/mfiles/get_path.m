function Path = get_path
%% Defines Paths for all tools
% The Path for neuralynx data is defined in get_experiment_list.m
Path.output = 'F:\Klusta\StimHP\';% matlab output (figures, mat files with results), Excel sheets, etc...
Path.temp   = 'F:\Klusta\StimHP\';% here we save temporary files (e.g. filtered signals) to speed up the routines, this folder has to be cleared after finishing project
end